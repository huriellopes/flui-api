# Copie para .env no host GPU.
# Domínio que aponta (DNS A) para o IP deste host GPU. Ex.: ollama.cantinbr.com.br
OLLAMA_DOMAIN=ollama.SEU-DOMINIO
# Token forte exigido em toda chamada (gere com: openssl rand -hex 32).
# Use o MESMO valor em OLLAMA_API_KEY no .env da API.
OLLAMA_TOKEN=CHANGE_ME
